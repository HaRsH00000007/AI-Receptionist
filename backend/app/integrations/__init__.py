"""Third-party integrations a tenant can connect.

``catalog`` says what exists and for whom; ``providers`` holds the adapters that
actually work; ``service`` is the only thing that reads or writes a tenant's
connection. See each module for its part of the contract.
"""
